# 複製這個檔案成 .env，填入你的 key。.env 已經在 .gitignore 裡，不會被 commit。
#
#   cp .env.example .env
#
# 三家只需要填「一家」。都填的話會照 anthropic → openai → gemini 的順序自動選，
# 或用 PROVIDER 明確指定。

# ── Gemini ─────────────────────────────────────────────
# 申請（有免費額度）：https://aistudio.google.com/apikey
#
# 注意：Gemini 的 API 有地區限制。在不支援的地區，任何 key 都會回
#   400 User location is not supported for the API use. [FAILED_PRECONDITION]
# 這不是 key 的問題，換 key 不會好，要換地區或改用其他 provider。
# （能看到這句話本身是 2026-09-09 修的：在那之前 SDK 把 body 丟掉，
#   只剩「400 status code (no body)」——見 shared/providers/errors.ts）
GEMINI_API_KEY=

# ── Anthropic (Claude) ─────────────────────────────────
# 申請：https://console.anthropic.com/
# ANTHROPIC_API_KEY=

# ── OpenAI ─────────────────────────────────────────────
# 申請：https://platform.openai.com/
# OPENAI_API_KEY=


# ── 選填 ───────────────────────────────────────────────
# 明確指定要用哪一家：anthropic | openai | gemini | fake
# PROVIDER=gemini

# 覆寫預設 model（預設 model 沒權限、回 404 時用這個）
# Gemini 可選：gemini-3.6-flash（預設）/ gemini-3.5-flash-lite（最便宜最快）
# MODEL=gemini-3.5-flash-lite
