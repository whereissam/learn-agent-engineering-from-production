# scratch notes

The parser rewrite is half done. `src/parse.ts` still trims the wrong end of the
token when the input has a trailing comma.

Deploys have started coming back 401. `deploy.sh` is the script that runs; the
credentials it uses are not in this repo.
